name: matrix

on: push

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Log node version 
        run: node -v 
      - name: Set node version      
        uses: actions/setup-node@v3
        with:
          node-version: 14
        run: node -v 
       